// Use the contextmenu event listener, check the cunstom data attribute view, folder, file, root, editor, right pane
// Load different context menu based on the view, 
// for files, use another data atrribute with filepath in it
// For multi selection have a index data type, and use the index to highlight the selection
// have isSelected and shiftSelected boolean data types on the file, for isSlected it will store individual index 
// for shiftSelected, it will loop through the indexes, from starting to ending index number 